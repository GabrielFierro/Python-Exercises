# Algoritmo que dada una estructura de tipo Lista cargada con elementos de tipo 'str', muestra por consola todos sus elementos mediante la estructura for.

lista = ["black", "yellow", "brown", "blue", "red"]

for x in lista:
    print(x)